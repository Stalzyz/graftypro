#!/usr/bin/expect -f
set timeout 60
set password "Photoshop09@"
set server "root@72.61.231.187"
set cmd [lindex $argv 0]

spawn ssh $server "cd /root/grafty_bsp && $cmd"
expect "password:"
send "$password\r"
expect eof
