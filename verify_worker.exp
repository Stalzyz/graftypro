#!/usr/bin/expect -f
set SERVER "root@72.61.231.187"
set PASS "Photoshop09@"
set timeout 10
spawn ssh -o StrictHostKeyChecking=no $SERVER "docker exec grafty_bsp-worker-1 cat /app/worker.ts | grep -n -A 5 'export const campaignWorker'"
expect {
    "password:" { send "$PASS\r"; exp_continue }
    eof
}
