#!/usr/bin/expect -f
set SERVER "root@72.61.231.187"
set PASS "Photoshop09@"
set timeout 30
spawn ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no $SERVER "pm2 describe send-grafty | grep 'script path'"
expect {
    "password:" { send "$PASS\r"; exp_continue }
    eof
}
