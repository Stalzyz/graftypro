#!/usr/bin/expect -f
set SERVER "root@72.61.231.187"
set PASS "Photoshop09@"
set timeout 10
spawn ssh -o StrictHostKeyChecking=no $SERVER "cat /root/grafty_bsp/docker-compose.prod.yml | grep -A 5 services"
expect {
    "password:" { send "$PASS\r"; exp_continue }
    eof
}
