#!/usr/bin/expect -f
set SERVER    "root@72.61.231.187"
set PASS      "Photoshop09@"
set timeout   60

spawn ssh -o StrictHostKeyChecking=no $SERVER "lsof -i :5432 || netstat -tlnp | grep 5432"
expect {
    "password:" { send "$PASS\r"; exp_continue }
    eof
}
